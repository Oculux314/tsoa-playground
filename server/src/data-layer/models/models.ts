export interface User {
  id: number;
  email: string;
  name: string;
  phoneNumbers: string[];
}

export interface Vehicle {
  id: number;
  make: string;
  model: string;
  year: number;
  color: string;
}
